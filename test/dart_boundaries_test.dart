// Integration tests for dart_boundaries rules.
// Rule-level unit tests are in test/utils/ and can be run via `dart test`.
// Full lint integration tests require a host project with the plugin enabled
// and are covered by the example/ project.
void main() {}
